const header=document.querySelector('.site-header');const toggle=document.querySelector('[data-menu-toggle]');const panel=document.querySelector('[data-mobile-panel]');const html=document.documentElement;const setScrolled=()=>header&&header.classList.toggle('scrolled',window.scrollY>50);window.addEventListener('scroll',setScrolled,{passive:true});setScrolled();if(toggle&&panel){toggle.addEventListener('click',()=>{const o=panel.classList.toggle('open');toggle.setAttribute('aria-expanded',String(o));html.style.overflow=o?'hidden':''})}document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');if(!id||id==='#')return;const t=document.querySelector(id);if(!t)return;e.preventDefault();t.scrollIntoView({behavior:'smooth'});panel?.classList.remove('open');html.style.overflow=''}));document.querySelectorAll('[data-lang-target]').forEach(b=>b.addEventListener('click',()=>{const t=b.getAttribute('data-lang-target');if(t)window.location.href=`${t}${window.location.hash||''}`}));