const f=document.querySelector('[data-waitlist-form]');if(f){const email=f.querySelector('input[type="email"]');const btn=f.querySelector('button[type="submit"]');const status=document.querySelector('[data-form-status]');const rx=/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/i;const conf=()=>{const c=document.createElement('canvas');c.style.cssText='position:fixed;inset:0;pointer-events:none;z-index:80';c.width=innerWidth;c.height=innerHeight;document.body.appendChild(c);const x=c.getContext('2d');const p=Array.from({length:120},()=>({x:Math.random()*c.width,y:-20-Math.random()*100,s:3+Math.random()*6,v:2+Math.random()*3,c:['#2E86C1','#10B981','#F59E0B','#D6EAF8'][Math.floor(Math.random()*4)]}));let f=0;const loop=()=>{x.clearRect(0,0,c.width,c.height);p.forEach(q=>{q.y+=q.v;x.fillStyle=q.c;x.fillRect(q.x,q.y,q.s,q.s*.7)});if(++f<140)requestAnimationFrame(loop);else c.remove()};loop()};f.addEventListener('submit',async e=>{e.preventDefault();if(!rx.test(email.value.trim())){status.textContent='Bitte geben Sie eine gueltige E-Mail-Adresse ein.';status.style.color='#fca5a5';return}btn.disabled=true;btn.textContent='Wird gesendet...';status.textContent='';try{const data=new FormData(f);const res=await fetch('https://api.web3forms.com/submit',{method:'POST',body:data});const j=await res.json();if(!res.ok||!j.success)throw new Error();f.innerHTML='<p style="font-size:1.1rem;color:#fff"><strong>Danke!</strong> Sie stehen jetzt auf der NetzVolt-Warteliste.</p>';conf()}catch{status.innerHTML='Senden fehlgeschlagen. Bitte an <a href="mailto:kontakt@netzvolt.de">kontakt@netzvolt.de</a> schreiben.';status.style.color='#fca5a5';btn.disabled=false;btn.textContent='Auf Warteliste setzen'}})}